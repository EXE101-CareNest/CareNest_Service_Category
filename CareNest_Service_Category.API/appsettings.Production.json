{
  "DatabaseSettings": {
    "Ip": "${DB_HOST}",
    "Port": "${DB_PORT}",
    "User": "${DB_USER}",
    "Password": "${DB_PASSWORD}",
    "Database": "${DB_NAME}"
  },
  "APIService": {
    "BaseUrlService": "${SERVICE_API_URL}",
    "BaseUrlShop": "${SHOP_API_URL}"
  },
  "Swagger": {
    "Enabled": false
  }
}
